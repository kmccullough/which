<ul class="menu">
  {{#each this.model.choices as |choice index|}}
    <li><LinkTo @route="choice" @model={{index}}>{{choice.name}}</LinkTo></li>
  {{/each}}
</ul>

<button type="button"
  {{on "click" (transition-to "choice-add")}}
  {{in-named-element "footer-right"}}
>
  Add Choice
</button>
